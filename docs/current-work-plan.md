# Current work plan

Repository type: reusable agency starter, not a client project yet.

Goal: create a reusable Astro + Sanity + Cloudflare Pages starter with documentation, SEO/schema components, integration placeholders, and safe client-editing guidance.

Plan:

1. Inspect existing repo structure.
2. Add missing monorepo app structure for `apps/web` and `apps/studio`.
3. Add starter docs, environment example, SEO/schema components, integration placeholders, and Cloudflare instructions.
4. Install dependencies and run available checks.
5. Summarize pass/fail results and next steps.
